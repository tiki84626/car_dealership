apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "car-dealership"
version = '1.0'
mainClassName = "Vehicle"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
